---
- name: Echo Test
  hosts: all

  tasks:
    - name: Print Echo Message
      debug: msg="Hello, world"
      
    - name: Print Foo
      debug: msg="Foo is {{ foo }}"
      
    - name: Print dt-env-url
      debug: msg="{{ dt-env-url }}"
